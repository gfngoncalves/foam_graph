version: 2

build:
   image: latest

sphinx:
  configuration: docs/source/conf.py

python:
   version: 3.9
   system_packages: true
   install:
      - requirements: docs/requirements.txt
      - method: setuptools
        path: .

formats: []
